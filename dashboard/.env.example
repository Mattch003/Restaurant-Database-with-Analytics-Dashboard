# Copy this file to .env and fill in your MySQL credentials.
# .env is gitignored so your password stays out of version control.

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD= "FILL IN PASSWORD"
DB_NAME=RestaurantOrderingSystemDB
PORT=3000
